# Report drafts

Continuously-drafted source material for the 40-page capstone report — per
`master-development-plan.md` §16 ("Report-evidence capture map") and the
per-phase DOCUMENT step: each phase drafts the report section its evidence
feeds, *while the material is fresh*, so P11 is an assembly + formatting pass
into the institution's template, not first-drafting from scratch.

**This is source material, not the final formatted document.** Plain
Markdown here; copy/adapt into the actual submission template (Times New
Roman, 14pt headings/12pt body, per-chapter figure/table numbering) at P11.

**Every status label follows CLAUDE.md's integrity rule** — Planned →
Implemented → Tested → Deployed → Measured → Verified. A claim in these
drafts carries the label it actually earned through real execution in this
repo, never an aspirational one.

## Chapter status

| Chapter | Status | Fed by | File |
|---|---|---|---|
| Project Description | Draft (covers P0-P7, no narrative gap; Deployment Flow narrative blocked on P10) | P0 topology figure, architecture recap; P1 event management, P2 browse/search, P3 booking + dual hold, P4 payment + synchronous call, P6 cancellation/refunds, P5 notification retry/DLQ, P7 frontend | `project-description.md` |
| Requirement Gathering | Draft (all seven roles tables complete; non-functional-requirements benchmark cross-reference closed) | P1.T5 (roles table), P2.T1 (publish-step amendment), P1 addendum (venue/seat-map write API), P3.T6 (booking roles table + delete-rule amendment), P4.T3 (`/pay` roles table), P6.T1 (`/cancel` roles table), P5.T1–T2 (Notification Service roles table — no protected routes), P7.T1 (new ticket-status roles table, frontend auth-split note), P8 benchmark (non-functional requirements) | `requirement-gathering.md` |
| Class Diagrams | Draft — all five services covered (Event Service + Search Service + Booking Service + Payment Service + Notification Service) | P1.T4, P2.T2–T4, P3.T1/T3–T6 (Booking Manager/Repository + TicketHoldStrategy), P4.T2–T6 (Payment Manager/Repository/Producer + Booking's `pay_booking`/`PaymentOutcomeConsumer`/`confirm_hold` additions), P6.T1–T3 (`cancel_booking`/`release_booking`/`EventRepository`/`BookingCancelledProducer`; `refund_payment`/`NotificationProducer`/`BookingCancelledConsumer`), P5.T1–T4 (`NotificationManager`/`NotificationConsumer`/`RetryConsumer`/`DlqConsumer`/`RetryPublisher`, no Repository — no DB; Booking's `_publish_confirmation_with_retry`, Payment's `publish_payment_confirmed`) | `class-diagrams.md` |
| Database Schema Design | Draft — all five services covered (`event_db`, seat-map docs, ES index, `booking_db`, `payment_db`, Notification Service's deliberate absence of one) | P1.T2 (ER + textual), P2.T2 (ES mapping), P3.T1 (`tickets`/`bookings` ER + textual), P4.T1/T2 (`tickets.price_cents` + `payment_db` ER + textual), P6.T1/T2 (`booking_db.events` + `payments.stripe_refund_id`/`refunded` status), P5.T1 (no schema — retry state carried on the Kafka message itself, §17 amendment) | `database-schema-design.md` |
| Feature Development Process | Draft (Measured) — P8 benchmark complete | P8 benchmark (measured), P3 hold-strategy design | `feature-development-process.md` |
| Testing Strategy | Draft (partial — 5 tiers: unit/integration, Kafka+ES, adversarial, P3 test-first concurrency + 2-pass review, P4 self-verification + live-testing-caught bugs, P6 self-verification + real Kafka-testcontainer infra bug + a CHECKPOINT `/pre-pr` fail-open bug, P5 self-verification + two-round CHECKPOINT `/pre-pr` review (round 2 catching a bug round 1's own fix introduced), P7 live-traffic-through-the-real-stack tier, run twice — curl-driven wire-level first (2 findings: a frontend OIDC audience-mapper gap, and a Phase-3-era crash on the double-booking-critical path itself), then a live Claude-in-Chrome rendered-browser walkthrough (3 more findings only a rendered UI pass could surface: a login-never-completes OIDC callback race, a seat-label concatenation display bug, and a nav-spacing misclick hazard) — all 5 found+fixed; a post-Phase-9 hardening arc, nine further adversarial/failure-injection/live-verification rounds against the running stack ahead of P10, 14 real bugs found+fixed plus one infrastructure defect (Kafka never actually persisting data), and Round 9's real Stripe test-mode charge-and-refund round trip closing the last deferred gap (the `refund_payment` replay-no-op idempotency branch) — no deliberately accepted gaps remain except the still-unfixed Postgres-down 500) | P1.T6 (`testcontainers` first use), P2.T3/T5 (Kafka consumer redelivery + eventual-consistency testing), pre-Phase-3 adversarial pass (8 rounds, 5 bugs found+fixed), P3.T3–T5/T7 (test-first `TicketHoldStrategy` contract + race proofs, both real strategies), P3 checkpoint (routine + dedicated adversarial `/code-review`, 2nd pass catching bugs in the 1st pass's own fixes), P4.T4/T7 (webhook + charge idempotency test-first, live-testing-caught idempotency bug + bind-param regression), P6.T4 (first real-Kafka-broker transport test in the project, surfaced a 3-phase-old broken-fixture bug), Phase 6 CHECKPOINT (`/pre-pr` review found a cancellation-cutoff fail-open bug plus 5 others), P5.T3–T4 (redelivery-safe-no-op test, structlog `capture_logs()` pattern, cross-test topic-leakage fix), Phase 5 CHECKPOINT (two-round `/pre-pr` review found a backoff overflow, an unguarded republish, a publish-before-commit rollback bug, and a DTO-tightening regression), P7.T1/T4 (real curl-driven OIDC login found a missing audience-mapper; a real two-client seat race found and fixed a `MissingGreenlet` crash present since Phase 3, plus the too-broad `except Exception` in the existing race test that had hidden it), Phase 7 CHECKPOINT (a live Claude-in-Chrome browser walkthrough found and fixed a login-completion race, a seat-label display bug, and a nav-spacing misclick hazard — `docs/build-log.md`, 2026-08-21), P9.T1 (Kafka idempotency coverage matrix across all five integration points, one new literal-redelivery test for point 4), post-P9 hardening rounds 1–9 (`docs/build-log.md`, 2026-08-22/23 entries — adversarial/sanity sweep, DRAFT/booking existence-oracle fixes, security/injection fuzzing, Kafka-persistence infra fix, DLQ + Kafka-down live checks, live Kafka-redelivery idempotency across all DB-writing consumers, 20-way/15-way concurrency races under both hold strategies, external-dependency-down sweep, JWT edge cases, live cancellation-cutoff enforcement, a real Stripe test-mode charge-and-refund round trip) | `testing-strategy.md` |
| Deployment Flow | Not started — blocked on P10 | P10.T1–T4 | — |
| Technologies Used | Draft (partial, running list) | every phase's tech, incl. P2's Kafka integration + Elasticsearch, P3's Redis + APScheduler, P8's Prometheus/Grafana + Python asyncio load harness, P7's React/Vite/TanStack Query/react-oidc-context, P9.T2 (log-level discipline audit, one real inconsistency found+fixed; corrected an overbroad `/metrics`-through-the-gateway claim) | `technologies-used.md` |
| Conclusion (Limitations/Future Work) | Not started — drafted last, P11.T3 | decisions-log §26 pull-list | — |
| Abstract | Not started — written last, P11.T1 | whole report | — |

Update this table every phase alongside the chapter files — it's the one
place that shows report progress at a glance without opening every file.
