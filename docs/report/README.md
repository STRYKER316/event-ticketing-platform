# Report drafts

Continuously-drafted source material for the 40-page capstone report — per
`master-development-plan.md` §16 ("Report-evidence capture map") and the
per-phase DOCUMENT step: each phase drafts the report section its evidence
feeds, *while the material is fresh*, so P11 is an assembly + formatting pass
into the institution's template, not first-drafting from scratch.

**This is source material, not the final formatted document.** Plain
Markdown here; copy/adapt into the actual submission template (Times New
Roman, 14pt headings/12pt body, per-chapter figure/table numbering) at P11.

**Every status label follows CLAUDE.md's integrity rule** — Planned →
Implemented → Tested → Deployed → Measured → Verified. A claim in these
drafts carries the label it actually earned through real execution in this
repo, never an aspirational one.

## Chapter status

| Chapter | Status | Fed by | File |
|---|---|---|---|
| Project Description | Draft (covers P0-P10, no narrative gap); lightly condensed 2026-08-25 (report length pass) — connective/framing prose tightened throughout, every phase's mechanism, cross-reference, and Status claim kept | P0 topology figure, architecture recap; P1 event management, P2 browse/search, P3 booking + dual hold, P4 payment + synchronous call, P6 cancellation/refunds, P5 notification retry/DLQ, P7 frontend, P10 AWS deployment | `project-description.md` |
| Requirement Gathering | Draft (all six roles tables complete; non-functional-requirements benchmark cross-reference closed); condensed 2026-08-25 (report length pass) — connective/repetitive prose around the tables tightened, every role, permission, endpoint status, bug narrative, and Measured benchmark number kept | P1.T5 (roles table), P2.T1 (publish-step amendment), P1 addendum (venue/seat-map write API), P3.T6 (booking roles table + delete-rule amendment), P4.T3 (`/pay` roles table), P6.T1 (`/cancel` roles table), P5.T1–T2 (Notification Service roles table — no protected routes), P7.T1 (new ticket-status roles table, frontend auth-split note), P8 benchmark (non-functional requirements) | `requirement-gathering.md` |
| Class Diagrams | Draft — all five services covered (Event Service + Search Service + Booking Service + Payment Service + Notification Service), all five diagrams rendered to PNG + SVG (`assets/diagrams/class-*.png`/`.svg`) from their Mermaid source; two bug narratives (Redis hold-sweep gap, Traefik `/payments/charge` bypass) trimmed 2026-08-23 to keep the resulting design/mechanism here and point to Testing Strategy for the finding itself, no content lost; each "Why this shape" section further condensed 2026-08-25 (report length pass) — every fact, number, test count, and Status/Verified claim kept, only connective/repetitive prose trimmed | P1.T4, P2.T2–T4, P3.T1/T3–T6 (Booking Manager/Repository + TicketHoldStrategy), P4.T2–T6 (Payment Manager/Repository/Producer + Booking's `pay_booking`/`PaymentOutcomeConsumer`/`confirm_hold` additions), P6.T1–T3 (`cancel_booking`/`release_booking`/`EventRepository`/`BookingCancelledProducer`; `refund_payment`/`NotificationProducer`/`BookingCancelledConsumer`), P5.T1–T4 (`NotificationManager`/`NotificationConsumer`/`RetryConsumer`/`DlqConsumer`/`RetryPublisher`, no Repository — no DB; Booking's `_publish_confirmation_with_retry`, Payment's `publish_payment_confirmed`), P7.T1 (Booking's `list_by_event`/`list_tickets_for_event` addition) | `class-diagrams.md` |
| Database Schema Design | Draft — all five services covered (`event_db`, seat-map docs, ES index, `booking_db`, `payment_db`, Notification Service's deliberate absence of one), all three Postgres ER diagrams rendered to PNG + SVG (`assets/diagrams/db-*.png`/`.svg`) from their Mermaid source; three bug narratives (Redis hold-sweep gap, `price_cents` bind-param overflow, `stripe_charge_id IS NULL` retry bug) trimmed 2026-08-23 to keep the resulting schema fact here and point to Testing Strategy for the finding itself, no content lost; prose further condensed 2026-08-25 (report length pass) — every constraint name, migration ID, column, number, and Status/Verified claim kept, only connective phrasing trimmed | P1.T2 (ER + textual), P2.T2 (ES mapping), P3.T1 (`tickets`/`bookings` ER + textual), P4.T1/T2 (`tickets.price_cents` + `payment_db` ER + textual), P6.T1/T2 (`booking_db.events` + `payments.stripe_refund_id`/`refunded` status), P5.T1 (no schema — retry state carried on the Kafka message itself, §17 amendment) | `database-schema-design.md` |
| Feature Development Process | Draft (Measured) — P8 benchmark complete; reviewed 2026-08-25 (report length pass) — left almost entirely intact given its Measured status, only two sentences of surrounding prose tightened, every benchmark number verified byte-for-byte unchanged | P8 benchmark (measured), P3 hold-strategy design | `feature-development-process.md` |
| Testing Strategy | Draft (partial — reorganized 2026-08-23 around four tiers plus a cross-cutting idempotency section, not phase chronology: Tiers 1/2 unit+integration (including P3's test-first concurrency methodology and the P9 Kafka-idempotency coverage matrix across all five integration points), Tier 3 adversarial testing (pre-P3 8-round arc), a "review gates" section covering P3's 2-pass review, P4/P6's CHECKPOINT-caught bugs, and P5's 2-round CHECKPOINT review (round 2 catching a bug round 1's own fix introduced), Tier 4 live-traffic-through-the-real-stack (P7, run twice — curl-driven wire-level first, then a live Claude-in-Chrome rendered-browser walkthrough — 5 findings total, all found+fixed), and a post-Phase-9 hardening arc (twelve further adversarial/failure-injection/live-verification rounds against the running stack ahead of P10, 15 real bugs found+fixed plus one infrastructure defect — Kafka never actually persisting data — Round 9's real Stripe test-mode charge-and-refund round trip closing the last deferred gap (the `refund_payment` replay-no-op idempotency branch), Round 10's genuine-concurrent-request and webhook-forgery checks (both clean), Round 11's real Stripe-minimum-charge finding — a ticket priced below $0.50 USD reached a real charge attempt and failed with an opaque 502, fixed with a DTO-boundary minimum on `price_cents` — and Round 12's live JWT-audience rejection and Notification Service poison-message checks, both clean), and a 2026-08-25 final sanity pass that closed the one remaining gap (the Postgres-down 500, fixed by scoping the catch to `ConnectionError`/`socket.gaierror`/`TimeoutError` rather than the `sqlalchemy.exc.OperationalError` first tried, which live testing showed doesn't actually catch a connect-time failure) — no deliberately accepted gaps remain; this chapter is now also the canonical home for the Redis hold-sweep-gap, Traefik `/payments/charge` bypass, `price_cents` bind-param overflow, and `stripe_charge_id IS NULL` retry bug narratives, each previously told in full in more than one chapter) | P1.T6 (`testcontainers` first use), P2.T3/T5 (Kafka consumer redelivery + eventual-consistency testing), pre-Phase-3 adversarial pass (8 rounds, 5 bugs found+fixed), P3.T3–T5/T7 (test-first `TicketHoldStrategy` contract + race proofs, both real strategies), P3 checkpoint (routine + dedicated adversarial `/code-review`, 2nd pass catching bugs in the 1st pass's own fixes), P4.T4/T7 (webhook + charge idempotency test-first, live-testing-caught idempotency bug + bind-param regression), P6.T4 (first real-Kafka-broker transport test in the project, surfaced a 3-phase-old broken-fixture bug), Phase 6 CHECKPOINT (`/pre-pr` review found a cancellation-cutoff fail-open bug plus 5 others), P5.T3–T4 (redelivery-safe-no-op test, structlog `capture_logs()` pattern, cross-test topic-leakage fix), Phase 5 CHECKPOINT (two-round `/pre-pr` review found a backoff overflow, an unguarded republish, a publish-before-commit rollback bug, and a DTO-tightening regression), P7.T1/T4 (real curl-driven OIDC login found a missing audience-mapper; a real two-client seat race found and fixed a `MissingGreenlet` crash present since Phase 3, plus the too-broad `except Exception` in the existing race test that had hidden it), Phase 7 CHECKPOINT (a live Claude-in-Chrome browser walkthrough found and fixed a login-completion race, a seat-label display bug, and a nav-spacing misclick hazard — `docs/build-log.md`, 2026-08-21), P9.T1 (Kafka idempotency coverage matrix across all five integration points, one new literal-redelivery test for point 4), post-P9 hardening rounds 1–12 (`docs/build-log.md`, 2026-08-22/23 entries — adversarial/sanity sweep, DRAFT/booking existence-oracle fixes, security/injection fuzzing, Kafka-persistence infra fix, DLQ + Kafka-down live checks, live Kafka-redelivery idempotency across all DB-writing consumers, 20-way/15-way concurrency races under both hold strategies, external-dependency-down sweep, JWT edge cases, live cancellation-cutoff enforcement, a real Stripe test-mode charge-and-refund round trip, forged-webhook-signature rejection, a genuinely concurrent 5-way `/pay` race against the same booking, pay-after-hold-expiry, cancel-while-still-PENDING, a real Stripe-minimum-charge DTO fix, live JWT wrong/missing-audience rejection via a temporary Keycloak client, and Notification Service poison-message handling) | `testing-strategy.md` |
| Deployment Flow | Deployed (Measured cost figures) — full topology, secrets, security groups, Budgets alert, validation run, stop/terminate discipline (incl. a real §12/§13 ASG correction), and cost writeup (now covering both the P10 deployment session and the P11.T5 demo-recording session), all live-verified against the real EB environment; lightly condensed 2026-08-25 (report length pass) — connective prose tightened in three spots, every AWS resource ID, dollar figure, timestamp, and Status/Verified claim kept (already dense with tables and facts, so the safe cut was modest) | P10.T1–T4, P11.T5 | `deployment-flow.md` |
| Technologies Used | Draft, running list; condensed 2026-08-25 (report length pass) — each entry trimmed to its essential what/why/verified-status claim, no fact or number dropped, only duplicate narration cut; Docker/Docker Compose and AWS Elastic Beanstalk added as their own dedicated entries (previously only referenced from Deployment Flow) covering what each technology is and why it was chosen, leaving the actual provisioning steps and live validation run in Deployment Flow | every phase's tech, incl. P2's Kafka integration + Elasticsearch, P3's Redis + APScheduler, P4's Stripe (live-verified charge+refund round trip, P9/Round 9), P8's Prometheus/Grafana + Python asyncio load harness, P7's React/Vite/TanStack Query/react-oidc-context, P9.T2 (log-level discipline audit, one real inconsistency found+fixed; corrected an overbroad `/metrics`-through-the-gateway claim), P10.T1–T4 (Docker/Compose containerization + Elastic Beanstalk deployment platform entries) | `technologies-used.md` |
| Conclusion (Takeaways/Applications/Limitations/Future Work) | Draft — full chapter written (takeaways, real-world applications, Limitations, Future Work lifted from §26) now that every other chapter is in its final state; Takeaways/Applications prose condensed 2026-08-25 (report length pass) — Limitations/Future Work bullets left intact (already dense facts), every claim kept | decisions-log §26 pull-list, whole report for takeaways/applications | `conclusion.md` |
| Demo Script | Verified — reproducible browse→login→hold→pay→confirm walkthrough, run live against the deployed EB environment on 2026-08-25 with a fresh seat and live-verified webhook-driven confirmation. Linked from the assembled report's Appendix B rather than reproduced in full there (2026-08-25 report length pass) — its screenshot evidence substantially overlaps Deployment Flow's own validation-run screenshots, and it isn't part of the institution template's required chapter list | P11.T5, P10.T1–T4 (deployed environment) | `demo-script.md` |
| Abstract | Draft — written now that every other chapter is in its final state; reviewed 2026-08-25 (report length pass) — already tight at 71 lines, no cut made | whole report | `abstract.md` |

Update this table every phase alongside the chapter files — it's the one
place that shows report progress at a glance without opening every file.
