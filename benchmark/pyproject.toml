[project]
name = "hold-benchmark"
version = "0.1.0"
description = "Concurrent-client load harness for the P8 hold-mechanism benchmark (§6) — standalone tool, not part of the services workspace, mirrors infra/kafka-smoke-test's pattern"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28",
    "asyncpg>=0.30",
    "redis>=5.0",
]

[tool.uv]
package = false
